# DEPRECATED — replaced by publish.yml
# This file is kept to avoid confusion from existing bookmarks/links.
# The active publish pipeline is in publish.yml and triggers on push to main.
name: _deprecated_pypi_release
on:
  workflow_dispatch:
    inputs:
      noop:
        description: 'This workflow is deprecated. Use publish.yml instead.'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. See publish.yml."
