# Legacy modules — kept for reference only.
# These are NOT used by the current controller and will not be maintained.
#
# forecast.py      — LSTM-based forecaster (replaced by predictor.py)
# boiler.py        — Shelly-based boiler control (replaced by controller.py + HA)
# event_checker.py — Google Calendar integration (replaced by calendar_manager.py)
# data_handler.py  — InfluxDB data pipeline (replaced by ha_data_collector.py)
